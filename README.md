# Task Dashboard Desktop
Google Sheet is read-only. Tasks are filtered by the `Designer` column. Use **Change Designer** anytime. Folder and ZIP paths are stored locally only. GitHub Actions builds the Windows installer.

Push to GitHub, then:
`git tag v1.0.0`
`git push origin v1.0.0`
